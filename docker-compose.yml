version: '3'

services:
  mysql:
    image: mysql:5.7.22
    container_name: mysql
    restart: unless-stopped
    tty: true
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: homestead
      MYSQL_USER: homestead
      MYSQL_PASSWORD: secret
      MYSQL_ROOT_PASSWORD: secret
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql

  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: php
    ports:
      - "9000:9000"

  composer:
    image: composer:latest
    container_name: composer
    working_dir: /var/www/html
    depends_on:
      - php
      - laravel

<<<<<<< HEAD
  frontend-vue:
    build:
      context: .
      dockerfile: "./src/frontend/Dockerfile"
    ports:
      - "8080:8080"
=======
  npm:
    build: 
      context: .
      dockerfile: ./src/frontend/Dockerfile
    container_name: npm
    ports: 
      - 8080:8080
>>>>>>> e9bd6ee40428b3d1e33f7403a9dbba551d0f0405

  artisan:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: artisan
    depends_on:
      - mysql
    working_dir: /var/www/html
    entrypoint: ['/var/www/html/artisan']